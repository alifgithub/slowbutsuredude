Run fast nmap scan: nmap -p- T5 -oA ip
clear commandL export TERM=xterm-color
Download linenum.sh

# Valentine Machine:
  check the ssl vulnerability, got the ssh-hey
  check the bash history: $ cat ~/.bash_history
  learn tmux -h

# Poison Machine:
  check LFI vulnerability
  crack th e passwd backup
  check vnc viewer on linpeas, OR
  check the command injection in User Agent

# Sunday Machine:
  find finger exploit
  change user sunny to sammy
  check sudo -l

# Brainfuck Machine:
  try wpscan 
  check smpt creds
  create id_rsa using orestis data
  learn RSA decrypt 

# Cronos Machine:
  check dns with nslookup, dig axfr
  do sql injection, then command injection 
  learn schedule service

# Nineveh Machine:
  check ssl cert
  try hydra
  command inject php
  check procmon service 
  
# Solidstate Machine:
  check nc to 4555
  learn dash service
  check james server vuln

# Kotarak Machine:
  check command injection
  upload shell 
  use impacket-secretdump
  check wget exploit

# Tatarsause Machine:
  upload file
  find . -writable
  sudo -l
  check backuperer timer service

# Blue Machine:
  check port 445
  exploit the ethernal blue

# Devel Machine:
  ftp login, upload aspx
  exploit kitrapod
  
  
  
